import React from 'react';
import styled from 'styled-components';
import UserTemplate from 'templates/UserTemplate';

const BudgetPage = () => (
  <UserTemplate>
    <div>This is budget template</div>
  </UserTemplate>
);

export default BudgetPage;
